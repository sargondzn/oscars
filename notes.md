* pd.read_csv(__, sep=None, engine='python')

* if you have created a new repository on github and want to add your existing project from vscode, use:
    git init
    git add 'file name'
    git commit -m "first commit"
    git branch -M main 
    git remote add origin 'link to your repo'
    git push -u origin main

*if you wish to create a new venv, use the following script on the terminal
    python -m venv .venv
don't forget to click on yes when a message comes asking you to select the new venv for the folder